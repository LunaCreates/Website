---
label: Hero Banner
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: Title
- name: image
  type: file
  config:
    maxSize: 64
  label: Image
  description: Make sure the image has an aspect ratio of 4:1
